services:
  website:
    image: nginx:alpine
    container_name: test-website
    restart: unless-stopped
    volumes:
      - ./site:/usr/share/nginx/html:ro
    networks:
      - proxy

networks:
  proxy:
    external: true